package ch04.homework;

public class Homework2 {

	public static void main(String[] args) {		
		
		// x가 1	일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		int x = 1;
		for ( int y = 1 ; y <= 10 ; y++ ){	 
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
		
		// x가 2	일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		x = 2;
		for ( int y = 1 ; y <= 10 ; y++ ){		
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
		
		// x가 3	일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		x = 3;
		for ( int y = 1 ; y <= 10 ; y++ ){		
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
		
		// x가 4일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		x = 4;
		for ( int y = 1 ; y <= 10 ; y++ ){		
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
		
		// x가 5일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		x = 5;
		for ( int y = 1 ; y <= 10 ; y++ ){		
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
		
		// x가 6	일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		x = 6;
		for ( int y = 1 ; y <= 10 ; y++ ){		
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
		
		// x가 7일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		x = 7;
		for ( int y = 1 ; y <= 10 ; y++ ){		
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
		
		// x가 8일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		x = 8;
		for ( int y = 1 ; y <= 10 ; y++ ){		
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
		
		// x가 9일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		x = 9;
		for ( int y = 1 ; y <= 10 ; y++ ){		
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
		
		// x가 1	0일 때 y가 1부터 10까지 변하면서
		// 4x+5y=60에 만족하는 값을 찾고 있으면 (x, y)로 출력한다.
		x = 10;
		for ( int y = 1 ; y <= 10 ; y++ ){		
			if ( 4 * x + 5 * y == 60 ) {
				System.out.println("(" + x + ", " + y + ")");
			}
		}
	}

}
